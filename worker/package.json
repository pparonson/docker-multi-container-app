{
    "scripts": {
        "start": "node ./index.js",
        "dev": "nodemon",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "redis": "^3.0.2"
    },
    "devDependencies": {
        "nodemon": "^2.0.7"
    }
}
